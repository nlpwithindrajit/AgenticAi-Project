# Core API
# The floor matters: FastAPI < 0.141 passes `on_startup` to Starlette's
# Router.__init__, which Starlette 1.x removed. Pairing an older FastAPI with a
# current Starlette dies on import with
# "Router.__init__() got an unexpected keyword argument 'on_startup'".
fastapi>=0.141
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

# Agent workflow
langgraph>=0.2.50
langchain>=0.3
langchain-anthropic>=0.3

# Observability
langfuse>=3.0

# HTTP
httpx>=0.27
