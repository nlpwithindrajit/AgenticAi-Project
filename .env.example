# Copy to .env and fill in. Everything here is optional at Milestone 1 —
# the graph runs end to end with stub search nodes and no keys at all.

ENVIRONMENT=local
LOG_LEVEL=INFO
ALLOWED_ORIGINS=["http://localhost:3000"]

# LLM — optional. Without it the Flight agent uses a deterministic search plan.
ANTHROPIC_API_KEY=
LLM_MODEL=claude-opus-5
LLM_EFFORT=medium

# Amadeus Self-Service (flights) — https://developers.amadeus.com
# Free test tier. Its inventory is limited and prices are not live, so expect
# fewer offers than production. Switch AMADEUS_BASE_URL for production keys.
AMADEUS_CLIENT_ID=
AMADEUS_CLIENT_SECRET=
AMADEUS_BASE_URL=https://test.api.amadeus.com

# Travel APIs (Milestones 3-4) — one provider each, no booking
HOTEL_API_KEY=
PLACES_API_KEY=
MAPS_API_KEY=

# Langfuse (Milestone 6)
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com
